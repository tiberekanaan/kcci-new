/**
 * @file
 * Behaviors for the kcci theme.
 */

(function($, _, Drupal) {
  Drupal.behaviors.kcci = {
    attach: function() {
      // kcci JavaScript behaviors goes here.
    }
  };
})(window.jQuery, window._, window.Drupal);
